export class Movie {
  constructor (public title: string, public length: number, public releaseDate: string,  public summary: string, public spoilerSummary: string, public director: string, public coverPhoto: string, public photos: string[], public link: string, public trailerUrl: string) { }
}
